import { ConfigDirective } from './config.directive';

describe('ConfigDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfigDirective();
    expect(directive).toBeTruthy();
  });
});
